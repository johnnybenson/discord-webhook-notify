{
  "name": "discord-webhook-notify",
  "version": "1.1.4",
  "description": "To be described in more detail later.",
  "main": "index.js",
  "scripts": {
    "package": "esbuild index.js --bundle --platform=node --target=node24 --format=cjs --sourcemap --outfile=dist/index.js --legal-comments=linked"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@actions/core": "^3.0.1",
    "@actions/github": "^9.1.1",
    "webhook-discord": "^3.7.8"
  },
  "devDependencies": {
    "esbuild": "^0.28.0"
  }
}
